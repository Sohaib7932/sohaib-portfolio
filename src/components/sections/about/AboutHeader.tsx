"use client";

import { SectionHeader } from "@/components/sections/shared/SectionHeader";

export function AboutHeader() {
  return (
    <SectionHeader
      eyebrow="About Me"
      title="More than code,"
      highlight="a craft."
      body="A Software Engineering graduate and front-end developer working in React, Next.js and Flutter. Below is how I think, the rules I work by, and the tools I reach for every day."
    />
  );
}
