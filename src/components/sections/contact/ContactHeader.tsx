"use client";

import { SectionHeader } from "@/components/sections/shared/SectionHeader";

export function ContactHeader() {
  return (
    <SectionHeader
      eyebrow="Get In Touch"
      title="Let's build"
      highlight="something real."
      body="Whether it's a freelance project, a full-time role, or just a quick question, send me a note. I read every message, and you'll get a reply within 1 day."
    />
  );
}
